# Wuwe documentation website

The Docusaurus site reads documentation directly from the repository-level
`docs/` directory. The `website/` directory contains only presentation,
navigation, and deployment configuration.

```bash
cd website
npm ci
npm start
```

Build the deployable site:

```bash
npm run build
```
